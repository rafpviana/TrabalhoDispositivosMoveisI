<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trabalhodispositivosmoveisi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TelaLoginActivity"
            android:label="Login">
            <intent-filter
                android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TelaCadastroActivity"
            android:label="Cadastro"/>
        <activity android:name=".TelaPerfilInformacoesPessoaisActivity"
            android:label="Perfil"/>
        <activity android:name=".TelaPerfilInformacoesFisicasActivity"
            android:label="Perfil"/>
        <activity android:name=".TelaPerfilNivelAtividadeActivity"
            android:label="Perfil"/>
        <activity android:name=".TelaPerfilActivity"
            android:label="Perfil"/>
        <activity android:name=".TelaEditarPerfilActivity"
            android:label="Editar Perfil"/>
        <activity android:name=".TelaMinhasAtividadesActivity"
            android:label="Minhas Atividades"></activity>
    </application>

</manifest>